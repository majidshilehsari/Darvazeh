services:
  darvazeh:
    build: .
    ports:
      - "8080:8080"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?Set a strong password}
      PUBLIC_DOMAIN: ${PUBLIC_DOMAIN:?Set the public hostname}
    volumes:
      - state:/data
    restart: unless-stopped
volumes:
  state:
