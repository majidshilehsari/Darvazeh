# مدیریت نسخه‌ها

## آخرین نسخهٔ نرم‌افزار: 0.1.0-preview.1

| فیلد | مقدار |
|---|---|
| آخرین نسخهٔ نرم‌افزار است؟ | بله؛ اولین انتشار آزمایشی |
| شاخه | `arena/01a080a2-darvazeh` |
| شمارهٔ کامیت نرم‌افزار | `70f364dd51b3f6a3ea1c2766c007ea00be49ea8f` |
| تاریخ و زمان کامیت ISO | `2026-09-08T11:22:28+00:00` |
| زمان ثبت نسخه برای پوش، هجری شمسی / تهران | ۱۷ شهریور ۱۴۰۵ ساعت ۱۴:۵۲:۲۷ |
| وضعیت تولید | منتشرشده در production نیست؛ نصب با کاربر |
| وضعیت تست | ۷ تست پنل موفق؛ بیلد Docker و اتصال واقعی تأیید نشده |

### تفاوت با نسخهٔ قبل

از README اولیه به نسخهٔ اولیهٔ پنل فارسی تک‌مدیر، Xray/WebSocket، Docker و Nginx، نشست مدیر و CSRF، تغییر UUID، ری‌استارت هسته، گزارش بدون اطلاعات اتصال و تست‌های HTTP رسیدیم. در نوبت انتشار، راهنمای Hostim و «دربارهٔ برنامه» اضافه شد؛ منطق بک‌اندِ نوشته‌شده در نوبت قبل تغییر نکرد.

### کامیت تحویل مستندات

این فایل و راهنمای ایجنت در یک کامیت مستندات **بعد از کامیت نرم‌افزار بالا** ثبت می‌شوند و با همان یک push ارسال می‌شوند. آخرین نسخهٔ کد همان SHA بالا است، ولی مبنای ادامهٔ کار نوک شاخه (شامل مستندات) است. برای زمان و SHA دقیق آخرین کامیت تحویل:

```sh
git log -1 --format='%H %cI %s'
git ls-remote origin refs/heads/arena/01a080a2-darvazeh
```

زمان بالا زمان ثبت نسخه برای انتشار است، نه تأیید دریافت سرور GitHub. موفقیت push باید از تطبیق SHA remote در گزارش نهایی احراز شود. تاریخچهٔ دقیق دریافت push در GitHub و خروجی ابزار است؛ این فایل پیش از تنها push نهایی آماده شده است.

## سابقه

| نسخه | کامیت | زمان | توضیح |
|---|---|---|---|
| اولیه | `cefc23f43da9a57778e69b85e383915293b60fd1` | 2026-09-08T10:49:09Z | README اولیه؛ نقطهٔ شروع مخزن |
| 0.1.0-preview.1 | `70f364dd51b3f6a3ea1c2766c007ea00be49ea8f` | `2026-09-08T11:22:28+00:00` | اولین نسخهٔ نرم‌افزار و راهنمای استقرار |

برای انتشار بعدی ابتدا این فایل و راهنمای ایجنت خوانده شوند؛ main/production دست‌نخورده بمانند. برای تغییر جدید بک‌اند یا داده، قانون تأیید پنج‌رقمی کاربر لازم است.
