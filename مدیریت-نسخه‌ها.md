# مدیریت نسخه‌ها

## آخرین تحویل — رابط UI 0.3.1-preview.1

- کامیت نرم‌افزار `de3e33e90d903fc31986fdc9d0a03191457b403b`؛ زمان ثبت تحویل پیش از پوش: ۱۷ شهریور ۱۴۰۵ ساعت ۲۳:۱۶ تهران (`2026-09-08T23:16:46+03:30`). این زمان تأیید push نیست؛ SHA نهایی پس از کامیت مستندات با remote تطبیق داده شود.
- شاخه فقط `arena/01a080a2-darvazeh`؛ آخرین remote پیش از این نوبت `e41cb4fd98739214533a7545abae51b4deffbe9e`. GitHub ابتدا تأیید شد؛ HEAD snapshot اولیه بود و fetch/reset --mixed فایل‌های موجود را بدون تغییر با remote هم‌تراز کرد.
- درخواست کاربر QR کنار لینک و هنگام تولید، تاریخ انقضا در فرم/جدول و درصد progress بود؛ سپس خواست تمام شود تا Rebuild کند. **هیچ تغییر بک‌اند/داده انجام نشد**؛ هسته و API هنوز 0.3.0-preview.1. رابط در هدر 0.3.1 و About با UI 0.3.1-preview.1 مشخص است.
- مبنای اعتبار قبلی حفظ شد: قبل از اولین اتصال، فرم فقط تاریخ تخمینی مشروط دارد؛ پس از اولین اتصال، جدول و ویرایش تاریخ دقیق/ثانیه تهران دارند. تاریخ ثابت از زمان ساخت پیاده نشده و ادعا نشود.
- qrcode-generator 1.4.4 MIT بدون تغییر در ابتدای app/app.js بسته‌بندی شد تا static allowlist یا CSP سرور تغییر نکند. مجوز کامل در asset و docs/THIRD-PARTY-LICENSES.md هست. بدون CDN یا سرویس QR؛ SVG DOM، حاشیه چهار ماژول، دانلود خصوصی SVG. QR کلاینت فقط baseline است و Fragment/Mux JSON را شامل نمی‌شود.
- بعد از ساخت موفق، تفاوت شناسه‌های قبل/بعد دستگاه تازه را پیدا کرده و پس از آزادشدن modal busy، پنجرهٔ QR/لینک باز می‌شود. QR روی هر دو جدول و legacy موجود است. درصدها کنار نوار با حفظ حجم‌ها و بدون درصد جعلی نامحدود/نامشخص.
- تست: ۳۴ Python PASS؛ jsdom + HTTP PASS (fixture صریح درصد۳۵/تاریخ قطعی/ردیف متصل)؛ Chromium + HTTP PASS شامل رمزگشایی تصویر واقعی QR توسط jsQR و تطبیق دقیق لینک، دانلود SVG، ساخت/ویرایش/مودال و موبایل. تصویر QR و فرم واقعی دیده شد. syntax/diff PASS.
- وابستگی‌های تست خارج repo زیر /home/user/ui-check/node_modules دوباره نصب شدند: jsdom/playwright/@sparticuz/chromium/jsqr/pngjs. NSS از al2023.tar.br inflate شد؛ LD_LIBRARY_PATH=/tmp/al2023/lib برای Chromium. ابزار تست browser به jsqr و pngjs نیز نیاز دارد.
- Docker، تونل Hostim و کلاینت واقعی گوشی در این نوبت تست نشد. production/Volume/Envs دست‌نخورده‌اند. بعد از تکمیل مستندات تنها یک push انجام شود؛ کاربر فقط Rebuild همین شاخه و مشاهدهٔ نسخهٔ رابط را انجام دهد. About زمان واقعی آماده‌سازی شمسی تهران را دارد.


## آخرین نسخهٔ نرم‌افزار: 0.3.0-preview.1

| فیلد | مقدار |
|---|---|
| آخرین نسخه است؟ | بله |
| شاخهٔ آزمایشی | `arena/01a080a2-darvazeh` |
| کامیت نرم‌افزار | `ec9bb2fc3c28c11d909c73ca19ffd55c1b4483ef` |
| تاریخ و زمان کامیت ISO | `2026-09-08T15:50:42+00:00` |
| زمان ثبت نسخه برای پوش، شمسی تهران | ۱۷ شهریور ۱۴۰۵ ساعت ۱۹:۲۰:۲۲ |
| تغییرات | UI تب‌دار/مودال/چراغ/progress؛ سهمیهٔ پایدار و انقضا؛ JSON کلاینت |
| تغییر داده | schema2 افزایشی، جدول usage مستقل از retention؛ بکاپ پیش از migration و JSON |
| سازگاری | UUID قبلی حفظ و دستگاه قدیمی نامحدود؛ bypass برای دستگاه محدود مسدود |
| مجوز بک‌اند | 59198 |
| تست | ۳۴ تست Python + jsdom + Chromium واقعی موفق؛ Hostim/Xray واقعی این نسخه هنوز نیازمند تست |

کامیت مستندات تحویل بعد از این کامیت ساخته می‌شود؛ برای ادامه از نوک همین شاخه استفاده کنید. SHA نهایی تحویل و زمان موفقیت دریافت push با remote تطبیق داده می‌شود و در پاسخ نهایی می‌آید. دو کامیت با یک push نهایی ارسال می‌شوند؛ main/production دست‌نخورده‌اند. تاریخ بالا زمان ثبت انتشار است، نه تأیید دریافت سرور GitHub.


## سابقهٔ نسخهٔ نرم‌افزار: 0.2.0-preview.1

| فیلد | مقدار |
|---|---|
| آخرین نسخه است؟ | خیر؛ با 0.3.0-preview.1 جایگزین شد |
| شاخهٔ آزمایشی | `arena/01a080a2-darvazeh` |
| کامیت نرم‌افزار | `fdda973eae88ad96300b0bccb93d6d0776398577` |
| تاریخ و زمان کامیت ISO | `2026-09-08T13:50:42+00:00` |
| زمان ثبت انتشار برای پوش، شمسی/تهران | ۱۷ شهریور ۱۴۰۵ ساعت ۱۷:۲۰:۳۷ |
| تغییر نسبت به preview.2 | دستگاه مستقل، تاریخچهٔ SQLite، رصد WS، زمان/حجم مشاهده‌شده، اعتماد امن به IP، کشور اختیاری، دفتر تست |
| سازگاری | UUID اصلی حفظ؛ migration افزایشی و backup تنظیمات؛ bypass با MONITOR_ENABLED=0 |
| مجوز | تأیید پنج‌رقمی 50955 کاربر |
| تست | ۲۴ تست Python و smoke رابط با jsdom/API واقعی موفق؛ Xray/Hostim end-to-end این نسخه تأیید نشده |
| محدودیت | IP واقعی نیازمند ingress تأییدشده؛ کشور نیازمند MMDB محلی؛ XHTTP فعال نشده |

این فایل و راهنمای ایجنت در کامیت تحویل مستندات بعدی ثبت می‌شوند. مبنای ادامهٔ کار نوک شاخه با هر دو کامیت است، نه rollback به کامیت نرم‌افزار بالا. هر دو کامیت با یک push نهایی ارسال می‌شوند؛ SHA نهایی و زمان دریافت واقعی GitHub در پاسخ تحویل و remote ref بررسی می‌شود. main/production تغییر نکرده‌اند.


## سابقهٔ نسخهٔ نرم‌افزار: 0.1.0-preview.2

| فیلد | مقدار |
|---|---|
| آخرین نسخه است؟ | خیر؛ با 0.2.0-preview.1 جایگزین شد |
| شاخه | `arena/01a080a2-darvazeh` |
| کامیت نرم‌افزار | `9c3c716d805e216b1a14441656315c00b3c349d9` |
| تاریخ و زمان کامیت | `2026-09-08T12:08:34+00:00` |
| زمان ثبت نسخه برای پوش / شمسی تهران | ۱۷ شهریور ۱۴۰۵ ساعت ۱۵:۳۸:۳۳ |
| تغییر | جداسازی صفحه از خطای startup، تشخیص امن محیط/داده/هسته، جلوگیری از ورود بدون رمز معتبر |
| حفاظت داده | بدون migration، بدون حذف یا بازنویسی فایل موجود خراب، بدون تعویض UUID خودکار |
| تست | ۱۳ تست موفق؛ Docker و تونل واقعی هنوز تأیید نشده |
| مجوز | کد تأیید کاربر 58317 و درخواست صریح در دسترس ماندن صفحه |

یک کامیت مستندات پس از کامیت نرم‌افزار بالا، شامل دفتر نسخه‌ها و راهنمای ایجنت است. مرجع ادامهٔ کار نوک همین شاخه است. SHA نهایی تحویل با `git log -1` و remote ref قابل شناسایی است؛ زمان ثبت بالا قبل از تنها پوش نهایی است، نه ادعای موفقیت آن. نسخهٔ مستقر در Hostim باید جداگانه با `/api/bootstrap` و بخش دربارهٔ برنامه بررسی شود.


## سابقهٔ نسخهٔ نرم‌افزار: 0.1.0-preview.1

| فیلد | مقدار |
|---|---|
| آخرین نسخهٔ نرم‌افزار است؟ | خیر؛ با preview.2 جایگزین شد |
| شاخه | `arena/01a080a2-darvazeh` |
| شمارهٔ کامیت نرم‌افزار | `70f364dd51b3f6a3ea1c2766c007ea00be49ea8f` |
| تاریخ و زمان کامیت ISO | `2026-09-08T11:22:28+00:00` |
| زمان ثبت نسخه برای پوش، هجری شمسی / تهران | ۱۷ شهریور ۱۴۰۵ ساعت ۱۴:۵۲:۲۷ |
| وضعیت تولید | منتشرشده در production نیست؛ نصب با کاربر |
| وضعیت تست | ۷ تست پنل موفق؛ بیلد Docker و اتصال واقعی تأیید نشده |

### تفاوت با نسخهٔ قبل

از README اولیه به نسخهٔ اولیهٔ پنل فارسی تک‌مدیر، Xray/WebSocket، Docker و Nginx، نشست مدیر و CSRF، تغییر UUID، ری‌استارت هسته، گزارش بدون اطلاعات اتصال و تست‌های HTTP رسیدیم. در نوبت انتشار، راهنمای Hostim و «دربارهٔ برنامه» اضافه شد؛ منطق بک‌اندِ نوشته‌شده در نوبت قبل تغییر نکرد.

### کامیت تحویل مستندات

این فایل و راهنمای ایجنت در یک کامیت مستندات **بعد از کامیت نرم‌افزار بالا** ثبت می‌شوند و با همان یک push ارسال می‌شوند. آخرین نسخهٔ کد همان SHA بالا است، ولی مبنای ادامهٔ کار نوک شاخه (شامل مستندات) است. برای زمان و SHA دقیق آخرین کامیت تحویل:

```sh
git log -1 --format='%H %cI %s'
git ls-remote origin refs/heads/arena/01a080a2-darvazeh
```

زمان بالا زمان ثبت نسخه برای انتشار است، نه تأیید دریافت سرور GitHub. موفقیت push باید از تطبیق SHA remote در گزارش نهایی احراز شود. تاریخچهٔ دقیق دریافت push در GitHub و خروجی ابزار است؛ این فایل پیش از تنها push نهایی آماده شده است.

## سابقه

| نسخه | کامیت | زمان | توضیح |
|---|---|---|---|
| اولیه | `cefc23f43da9a57778e69b85e383915293b60fd1` | 2026-09-08T10:49:09Z | README اولیه؛ نقطهٔ شروع مخزن |
| 0.1.0-preview.1 | `70f364dd51b3f6a3ea1c2766c007ea00be49ea8f` | `2026-09-08T11:22:28+00:00` | اولین نسخهٔ نرم‌افزار و راهنمای استقرار |

برای انتشار بعدی ابتدا این فایل و راهنمای ایجنت خوانده شوند؛ main/production دست‌نخورده بمانند. برای تغییر جدید بک‌اند یا داده، قانون تأیید پنج‌رقمی کاربر لازم است.

### یادداشت بررسی، بدون انتشار جدید — ۱۷ شهریور ۱۴۰۵

بررسی read-only و اجرای محلی startup/HTTP انجام شد؛ با محیط معتبر صفحهٔ ورود پاسخ 200 دارد و با محیط نامعتبر توقف پیش از ساخت HTTP server بازتولید شد. راهنمای ایجنت تکمیل شد. نسخهٔ نرم‌افزار و remote همچنان `0.1.0-preview.1` / `216bf5b` هستند؛ این یادداشت تغییر بک‌اند یا انتشار جدید نیست. اصلاح تشخیص runtime منتظر تأیید کاربر است.
