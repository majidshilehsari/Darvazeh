# راهنمای ایجنت بعدی

## آخرین تحویل — رابط UI 0.3.1-preview.1

- کامیت نرم‌افزار `de3e33e90d903fc31986fdc9d0a03191457b403b`؛ زمان ثبت تحویل پیش از پوش: ۱۷ شهریور ۱۴۰۵ ساعت ۲۳:۱۶ تهران (`2026-09-08T23:16:46+03:30`). این زمان تأیید push نیست؛ SHA نهایی پس از کامیت مستندات با remote تطبیق داده شود.
- شاخه فقط `arena/01a080a2-darvazeh`؛ آخرین remote پیش از این نوبت `e41cb4fd98739214533a7545abae51b4deffbe9e`. GitHub ابتدا تأیید شد؛ HEAD snapshot اولیه بود و fetch/reset --mixed فایل‌های موجود را بدون تغییر با remote هم‌تراز کرد.
- درخواست کاربر QR کنار لینک و هنگام تولید، تاریخ انقضا در فرم/جدول و درصد progress بود؛ سپس خواست تمام شود تا Rebuild کند. **هیچ تغییر بک‌اند/داده انجام نشد**؛ هسته و API هنوز 0.3.0-preview.1. رابط در هدر 0.3.1 و About با UI 0.3.1-preview.1 مشخص است.
- مبنای اعتبار قبلی حفظ شد: قبل از اولین اتصال، فرم فقط تاریخ تخمینی مشروط دارد؛ پس از اولین اتصال، جدول و ویرایش تاریخ دقیق/ثانیه تهران دارند. تاریخ ثابت از زمان ساخت پیاده نشده و ادعا نشود.
- qrcode-generator 1.4.4 MIT بدون تغییر در ابتدای app/app.js بسته‌بندی شد تا static allowlist یا CSP سرور تغییر نکند. مجوز کامل در asset و docs/THIRD-PARTY-LICENSES.md هست. بدون CDN یا سرویس QR؛ SVG DOM، حاشیه چهار ماژول، دانلود خصوصی SVG. QR کلاینت فقط baseline است و Fragment/Mux JSON را شامل نمی‌شود.
- بعد از ساخت موفق، تفاوت شناسه‌های قبل/بعد دستگاه تازه را پیدا کرده و پس از آزادشدن modal busy، پنجرهٔ QR/لینک باز می‌شود. QR روی هر دو جدول و legacy موجود است. درصدها کنار نوار با حفظ حجم‌ها و بدون درصد جعلی نامحدود/نامشخص.
- تست: ۳۴ Python PASS؛ jsdom + HTTP PASS (fixture صریح درصد۳۵/تاریخ قطعی/ردیف متصل)؛ Chromium + HTTP PASS شامل رمزگشایی تصویر واقعی QR توسط jsQR و تطبیق دقیق لینک، دانلود SVG، ساخت/ویرایش/مودال و موبایل. تصویر QR و فرم واقعی دیده شد. syntax/diff PASS.
- وابستگی‌های تست خارج repo زیر /home/user/ui-check/node_modules دوباره نصب شدند: jsdom/playwright/@sparticuz/chromium/jsqr/pngjs. NSS از al2023.tar.br inflate شد؛ LD_LIBRARY_PATH=/tmp/al2023/lib برای Chromium. ابزار تست browser به jsqr و pngjs نیز نیاز دارد.
- Docker، تونل Hostim و کلاینت واقعی گوشی در این نوبت تست نشد. production/Volume/Envs دست‌نخورده‌اند. بعد از تکمیل مستندات تنها یک push انجام شود؛ کاربر فقط Rebuild همین شاخه و مشاهدهٔ نسخهٔ رابط را انجام دهد. About زمان واقعی آماده‌سازی شمسی تهران را دارد.


## آخرین تحویل — 0.3.0-preview.1 (مقدم بر تمام سوابق زیر)

- تأیید **59198** برای بازطراحی UI و تغییر بک‌اند سهمیه/انقضا دریافت شد؛ پیام بعدی «ادامه بده و تمومش کن» ادامهٔ همان محدوده است.
- آخرین کامیت نرم‌افزار: `ec9bb2fc3c28c11d909c73ca19ffd55c1b4483ef`؛ زمان کامیت `2026-09-08T15:50:42+00:00`؛ زمان ثبت نسخه برای پوش، تهران: ۱۷ شهریور ۱۴۰۵ ساعت ۱۹:۲۰:۲۲.
- شاخه فقط `arena/01a080a2-darvazeh`. یک کامیت مستندات بعد از این کامیت، نوک شاخه است؛ SHA نهایی از `git log -1` و remote ref مشخص شود. هر دو با یک push نهایی فرستاده می‌شوند؛ متن فایل پیش از push نوشته شده است.
- snapshot این نوبت نیز فایل‌ها را از آخرین remote داشت ولی HEAD محلی روی initial بود. بعد از fetch همان شاخه و reset --mixed، فایل‌ها حفظ و status خالی تأیید شد. هیچ branch دیگری ساخته یا production تغییر نکرد.

### رابط جدید

- HTML/CSS/JS جدا در `app/index.html`, `app/style.css`, `app/app.js`؛ سرور فقط همین مسیرهای static را allowlist می‌کند؛ CSP script-src self است.
- تب‌های هدر، جدول هم‌تراز، جست‌وجو، تب واقعاً متصل، progress سهمیه، چراغ + متن، پیام زیر هدر، مودال داخلی تمام ویرایش/تأییدها. native alert/confirm/prompt حذف شده‌اند.
- Escape/focus، حالت busy و خطای درون مودال، responsive و ARIA اعمال شده‌اند. اطلاعات حساس لینک فقط در مودال/خروجی خصوصی است و localStorage استفاده نشده است.
- مودال تنظیم کلاینت، **JSON مستقل Xray-core** می‌سازد، نه تنظیم خودکار گوشی/v2rayN. پیش‌فرض Fragment/Mux خاموش، TLS بررسی روشن، SOCKS فقط loopback:10818. مقادیر نمونهٔ Fragment از docs هستند، نه تضمین اپراتور. هیچ خروجی XHTTP فعال نشده.

### سهمیه و داده

- `usage` جدول مستقل و schema2؛ بودجهٔ مشترک تمام اتصال‌های هر دستگاه، رزرو تراکنشی قبل از ارسال؛ حذف تاریخچه، restart یا تعویض کلید آن را صفر نمی‌کند. مبلغ رزروشده‌ای که پیش از ارسال crash شده ممکن است حساب شود؛ معیار wire با سربار است نه تحویل فایل.
- migration نسخه۱: بکاپ `history-before-quota-v2.sqlite3`، seed یک‌باره از تاریخچهٔ باقی‌مانده با basis=retained-history. زمان/حجم حذف‌شدهٔ قبلی قابل بازیابی نیست. schema ناشناخته به‌جای بازسازی، رد می‌شود.
- تنظیمات quota_bytes و duration_days به دستگاه‌ها افزوده شده؛ legacy_* متناظر روی ریشهٔ settings است. فیلد غایب یعنی صفر/نامحدود؛ دستگاه قبلی خودکار محدود نمی‌شود. مودال تازه ۵GB ده‌دهی و۱۰روز پیشنهاد می‌کند.
- اعتبار از اولین مشاهدهٔ ثبت‌شده، روز تقویمی است؛ نه زمان ساخت و نه مجموع آنلاین. تغییر سقف/مدت از همان first_seen محاسبه می‌شود و reset صورت نمی‌گیرد. تمدید آگاهانه توسط مدیر با زیادکردن مدت یا سقف ممکن است.
- حجم روی هر قطعه و انقضای بیکار تقریباً هر ثانیه کنترل می‌شود. سهمیهٔ تمام‌شده تمام socketهای همان دستگاه را می‌بندد؛ دیگر دستگاه‌ها قطع نمی‌شوند. تغییر مدیریت دستگاه هنوز نیازمند restart هسته و قطع همهٔ اتصال‌های جاری است؛ UI هشدار دارد.
- خطای شمارنده برای دستگاه محدود fail-closed است. **MONITOR_ENABLED=0 فقط نامحدودها را وارد config Xray می‌کند**؛ دستگاه محدود با bypass آزاد نمی‌شود. API ساخت/ویرایش محدودیت در نبود پایش/DB رد می‌شود.
- اگر دستگاه دارای سقف/مدت وجود دارد، Early Data هدر Sec-WebSocket-Protocol رد می‌شود. prefix شناخته‌نشده/نامعتبر قبل از forward داده متوقف می‌شود. این سیاست ممکن است لینک دستی Early Data دستگاه نامحدود را نیز در محیط محدود مختل کند؛ لینک معمول پنل ed ندارد.
- بکاپ JSON جدید `backups/settings-before-0.3.0.json` افزایشی است؛ قدیمی‌ها حذف نشده‌اند. تنها یک replica پشتیبانی می‌شود. rollback به ایمیج قدیمی می‌تواند سهمیه را نادیده بگیرد یا schema2 را نشناسد؛ قبل از rollback سیاست محدودیت/داده با کاربر بحث شود.

### تست و تحویل

- **۳۴ تست Python موفق**: migration یک‌باره، concurrency، persistence، quota/expiry روی سوکت واقعی، سالم‌ماندن دستگاه دیگر، Early Data و meter failure، bypass config، API و security قبلی.
- **Chromium واقعی** با playwright و باینری npm @sparticuz/chromium اجرا شد. کتابخانه‌های NSS از al2023.tar.br همان پکیج استخراج شدند؛ apt دسترسی نداشت. آرگومان‌های disable-web-security و allow-running-insecure-content در تست حذف شدند.
- تست `tests/browser-smoke.cjs`: ورود/تب/مودال ساخت‌ویرایش‌لغو/جست‌وجو/لینک/progress/export کلاینت/Escape/mobile و نبود JS error/native dialog؛ موفق. تصاویر desktop/modal/mobile در /tmp ساخته و بررسی شدند. این تست واقعی مرورگر و API است، نه ساخت تصویر مصنوعی.
- `tests/ui-smoke.cjs` برای رابط جدید به‌روز و موفق شد. دستورهای توسعه: `NODE_PATH=/home/user/ui-check/node_modules node tests/ui-smoke.cjs` و `LD_LIBRARY_PATH=/tmp/al2023/lib NODE_PATH=/home/user/ui-check/node_modules node tests/browser-smoke.cjs`.
- syntax Python/JS/Bash و diff check موفق. **Docker build، اجرای JSON کلاینت با باینری Xray و تونل واقعی Hostim نسخه جدید تأیید نشده‌اند**؛ تست socket با peer کنترل‌شده جای آن‌ها نیست.
- پیش‌نمایش نمایشی روی 8083 با رمز غیرمحرمانه demo-password-2026 اجرا شده است. فایل‌های دادهٔ آزمایش در /tmp هستند و وارد Git نشده‌اند.
- راهنمای فعلی `docs/QUOTAS-AND-UI.fa.md` بر توصیه‌های bypass و محدودیت نسخه‌های قبلی اولویت دارد. کاربر باید Rebuild همان شاخه را انجام و نسخه 0.3.0-preview.1 را در پنل بررسی کند؛ Volume حذف نشود.

## آخرین تحویل — 0.2.0-preview.1 (مرجع فعلی، قبل از سوابق)

- مجوز توسعهٔ بک‌اند/SQLite با کد **50955** دریافت شد؛ کاربر لینک جدا برای هر دستگاه و نگهداری ۷روزه را پذیرفت. تعداد دستگاه‌ها مشخص نشد؛ سقف حفاظتی ۵۰ دستگاه و ۲۵۶ اتصال است، نه ظرفیت بنچمارک‌شده.
- نرم‌افزار: `fdda973eae88ad96300b0bccb93d6d0776398577`؛ زمان کامیت `2026-09-08T13:50:42+00:00`؛ زمان ثبت انتشار تهران: ۱۷ شهریور ۱۴۰۵ ساعت ۱۷:۲۰:۳۷؛ شاخه همچنان `arena/01a080a2-darvazeh`.
- یک کامیت مستندات بعد از این کامیت نوک شاخه خواهد بود؛ SHA تحویل را با `git log -1` و remote تطبیق بده. این فایل پیش از تنها push نهایی نوشته شده است.
- ابتدای این نوبت snapshot فایل‌ها نسخهٔ aa60cb6 را داشت، ولی git HEAD محلی cefc23f بود. پس از fetch همان شاخه، `git reset --mixed FETCH_HEAD` فقط index/HEAD را با remote هم‌تراز کرد؛ working tree حفظ و diff خالی تأیید شد. شاخهٔ دیگری ساخته نشد.

### اجزای جدید

- `app/telemetry.py`: SQLite history، رکورد اتصال، سیاست retention، تحلیل محدود ۱۷ بایت اول VLESS در WS و relay روی loopback:10001.
- Nginx `/connect` → relay → Xray:10000. متغیر `MONITOR_ENABLED=0` و Restart، entrypoint را به upstream قدیمی مستقیم برمی‌گرداند؛ بدون حذف داده یا عوض‌کردن کلید.
- `settings.json` منبع دستگاه‌هاست: UUID اصلی به عنوان legacy حفظ شده؛ devices افزایشی هستند. بکاپ خصوصی و یک‌بارنویس `backups/settings-before-0.2.0.json` پیش از apply ساخته می‌شود. نام/فعال‌سازی/کلید دستگاه در این نسخه نیازمند restart هسته و قطع اتصال‌هاست.
- DB جدا `history.sqlite3`، schema1، WAL؛ خطای DB روی پنل دیده می‌شود و هیچ دادهٔ خراب/نسخهٔ ناشناخته‌ای خودکار جایگزین نمی‌شود. default report تاریخچه/IP/UUID/نام دستگاه را ندارد؛ export تاریخچهٔ خصوصی جدا و نیازمند تأیید UI است.
- ثبت UUID در جدول تاریخچه وجود ندارد؛ شناسهٔ داخلی device_id ذخیره می‌شود. مدل/مالک دستگاه خودکار تشخیص داده نمی‌شود. مشاهدهٔ UUID معتبر در آغاز WS اثبات موفقیت مقصد یا حضور انسان نیست. تعداد اتصال باز برابر تعداد کاربر نیست.
- حجم شامل فریم WS و بایت مشاهده‌شده در relay است؛ نه payload خالص Xray یا هزینهٔ ISP. heartbeat هر۱۵ثانیه، crash → interrupted با آخرین مشاهده؛ پایان دقیق جعل نمی‌شود. Early Data منتقل ولی در آمار این نسخه منتسب نمی‌شود.
- XFF پیش‌فرض مورد اعتماد نیست. فقط CIDR رسمی ingress در `TRUSTED_PROXY_CIDRS` مجاز به اعتماد است؛ Nginx هدرهای داخلی را overwrite می‌کند و relay زنجیره را از راست بررسی می‌کند. بدون تأیید، IPکاربر/کشور نامشخص و peer به عنوان واسط نمایش داده می‌شود. هیچ CIDR عمومی/خصوصی گسترده را از خودت حدس نزن.
- کشور از MMDB محلی اختیاری `/data/GeoLite2-Country.mmdb`؛ بسته python3-maxminddb به Docker اضافه شد. دیتاست داخل Git/ایمیج نیست و IP به API خارجی ارسال نمی‌شود.
- retention هفت روز، سقف۵۰هزار اتصال بسته و۱۰۰۰ تست؛ purge فقط سوابق بسته/تست، نه دستگاه‌ها. حذف SQLite منطقی است، نه تضمین پاک‌سازی forensic. replica باید۱ باشد.

### پروفایل و پژوهش

- منابع رسمی WS و بحث XHTTP شماره4113 در تاریخ 2026-09-08 خوانده شد. توصیهٔ رسمی XHTTP مستند است ولی سازگاری Hostim تست نشده.
- baseline WS قابل استفاده، client-tuning فقط راهنمای دستی، XHTTP با برچسب **not-deployed**. پروتکل فعلی/نسخهٔ Xray تغییر نکرد. «آخرین/بهترین برای همه» ادعا نکن. دفتر نتایج تست دستی است نه benchmark خودکار.
- برای فعال‌سازی پروتکل جدید، بحث و تأیید جدید بک‌اند لازم است؛ نباید برچسب پژوهشی را با قابلیت پیاده‌شده اشتباه گرفت.

### آزمایش و تحویل

- ۲۴ تست Python موفق: API واقعی demo/production-startup، حفظ legacy/backup، CSRF/ورود، عدم افشای اطلاعات، retention/recovery، XFF spoofing، parsing فریم و relay با سوکت واقعی به **peer کنترل‌شدهٔ آزمایشی، نه Xray واقعی**.
- `tests/ui-smoke.cjs` با jsdom و HTTP واقعی پنل: ورود، دستگاه، لینک، پروفایل‌ها، تاریخچهٔ خالی بدون جعل؛ موفق. jsdom توسعه‌ای بیرون repo نصب شد؛ دستور: `NODE_PATH=/home/user/ui-check/node_modules node tests/ui-smoke.cjs`.
- syntax Python/Bash/JS و diff check موفق. Docker موجود نبود؛ دانلود Xray از release-assets و Chromium از CDN با خطای TLS مواجه شدند. تست GUI تصویری، build کامل و اتصال واقعی Xray این نسخه را موفق اعلام نکن.
- پیش‌نمایش نمایشی روی پورت8082 با رمز غیرمحرمانه `demo-password-2026` اجرا شد؛ هرگز رمز تولید نیست.
- کاربر موفقیت اتصال **نسخهٔ قبلی** را گزارش کرد؛ نسخهٔ پیشرفته هنوز توسط او روی Hostim تست نشده است. راهنمای جدید `docs/ADVANCED-PANEL.fa.md` قبل از ارتقا خوانده شود، بکاپ/اپ آزمایشی توصیه شود.

## آخرین تحویل — 0.1.0-preview.2 (مقدم بر یادداشت‌های تاریخی پایین)

- تأیید کاربر با کد `58317` دریافت شد؛ علاوه بر تشخیص امن خواست صفحه حتی هنگام خطای تنظیمات باز بماند.
- شاخه: `arena/01a080a2-darvazeh`؛ آخرین کامیت نرم‌افزار: `9c3c716d805e216b1a14441656315c00b3c349d9`؛ تاریخ کامیت: `2026-09-08T12:08:34+00:00`.
- زمان ثبت انتشار / تهران: ۱۷ شهریور ۱۴۰۵ ساعت ۱۵:۳۸:۳۳. نوک شاخه یک کامیت مستندات بعد از این کامیت است؛ برای ادامه از HEAD شاخه و `git ls-remote` استفاده کن، نه نسخهٔ قدیمی پایین.
- ENV_ERRORS دیگر SystemExit ایجاد نمی‌کند؛ صفحه و healthcheck زنده می‌مانند. bootstrap عمومی فقط version، login_enabled، کدهای ENV و initializing دارد، نه مقادیر. ورود با رمز کوتاه/خالی همچنان غیرممکن است.
- آماده‌سازی داده و Xray در thread پس‌زمینه است. خرابی دادهٔ موجود آن را بازنویسی نمی‌کند؛ فایل جدید فقط در نبود فایل قبلی و معتبر بودن محیط ساخته می‌شود. کنترل API و لینک تا آماده‌شدن مسدود است.
- خطای هسته فقط پس از ورود در status/report دیده می‌شود؛ raw stderr/stdout منتشر نمی‌شود. حالت missing Xray واقعی در تست‌ها با PATH خالی بررسی شده، نه هستهٔ جعلی.
- ۱۳ تست HTTP موفق، py_compile و bash -n و node --check و diff check موفق. تست Docker/Hostim واقعی انجام نشده است؛ نمایش صفحه جای تست VPN را نمی‌گیرد.
- فایل‌های تولید Hostim و Volume دست‌نخورده‌اند؛ migration یا تغییر UUID موجود انجام نشده. شروع نسخهٔ جدید نیاز به Build/Deploy همان شاخه دارد، نه حذف اپ/Volume.
- پیام قدیمی `Set ADMIN_PASSWORD ...` در کد جدید وجود ندارد؛ اگر پس از Deploy همچنان تکرار شد، زمان لاگ و شاخه/ایمیج اجراشده را بررسی کن.
- پیشنهادهای پایین که منتظر تأیید هستند تاریخی‌اند؛ تأیید و اصلاح این نوبت انجام شد. تغییر بک‌اند بعدی همچنان تأیید جدید کاربر می‌خواهد.

## ابتدا بخوان

- مخزن واقعی این سشن: `majidshilehsari/Darvazeh` در `/home/user/Darvazeh`؛ نام‌ها و ادعاهای ریپوهای قبلی در متن گفتگو را با وضعیت واقعی مخزن اشتباه نگیر.
- فقط شاخهٔ `arena/01a080a2-darvazeh` مجاز است. این شاخه نقش preview دارد؛ main/production را تغییر نده، شاخهٔ دیگری نساز و به آن پوش نکن.
- قبل از هر کار اتصال GitHub را با `gh auth status` و دسترسی remote را بررسی کن؛ در صورت خطای احراز هویت توقف و درخواست اتصال مجدد GitHub در Arena کن. تضمین دائمی‌بودن اتصال ممکن نیست.
- قبل از تغییر، آخرین remote ref و تاریخچه را بررسی و فایل `مدیریت-نسخه‌ها.md` را بخوان. ابتدا وضعیت working tree را بررسی کن؛ هیچ تغییر کاربر را بازنویسی نکن.

## آخرین نسخهٔ نرم‌افزار

- نسخه: `0.1.0-preview.1`
- آخرین کامیت تغییر نرم‌افزار: `70f364dd51b3f6a3ea1c2766c007ea00be49ea8f`
- تاریخ کامیت: `2026-09-08T11:22:28+00:00`
- زمان ثبت انتشار به تقویم شمسی / تهران: ۱۷ شهریور ۱۴۰۵ ساعت ۱۴:۵۲:۲۷
- یک کامیت مستندات تحویل پس از این کامیت ساخته می‌شود؛ آن کامیت فقط این راهنما و دفتر نسخه‌ها را اضافه می‌کند. مرجع ادامهٔ کار **نوک همین شاخه** است، نه checkout روی کامیت نرم‌افزار قدیمی‌تر.
- SHA دقیق آخرین کامیت تحویل را با `git log -1` و `git ls-remote origin refs/heads/arena/01a080a2-darvazeh` پیدا کن. هش یک کامیت را نمی‌توان داخل محتوای همان کامیت درج کرد؛ دفتر نسخه‌ها آخرین کامیت نرم‌افزار را ثبت می‌کند.
- قبل از این انتشار، تنها remote ref شاخهٔ main و کامیت `cefc23f43da9a57778e69b85e383915293b60fd1` بود (پوش ثبت‌شدهٔ 2026-09-08T10:49:09Z).

## درخواست و خطوط قرمز کاربر

- کاربر راهنمای فارسی ساده، قدم‌به‌قدم و پوش نهایی **فقط یک بار در هر نوبت انتشار** می‌خواهد. تولید/production به خود کاربر سپرده شده است.
- پاسخ‌ها: ۱) جدول چک‌لیست با 🟢/❌ و درصد انجام/باقی‌مانده ۲) چی فهمیدی ۳) کارهای باقی‌مانده ۴) سؤال‌های توسعه ۵) کیفیت پرامپت.
- برای هر **تغییر جدید بک‌اند یا داده** قبل از ویرایش توقف کن: عنوان «وضعیت اضطراری» نیاز به تغییر در دیتابیس و بک اند»، درک تغییر، ابهام‌ها، قدم بعد و کد تصادفی پنج‌رقمی را ارائه کن و منتظر تأیید همان کد بمان. این نوبت بک‌اند جدید تغییر نکرد؛ کد نوشته‌شده در نوبت قبل با درخواست صریح کاربر منتشر شد.
- حذف Volume، migration، overwrite داده، یا تغییر اپ قبلی بدون بحث و تأیید ممنوع. پروژه فعلاً SQL ندارد؛ فایل‌های `/data` هم دادهٔ حساس‌اند.
- با هر انتشار بخش «دربارهٔ برنامه» و اطلاعات نسخه/زمان شمسی تهران را به‌روز کن. زمان آماده‌سازی نسخه را با زمان موفقیت واقعی push اشتباه نکن.

## ساختار فنی و محدودیت‌ها

- Python stdlib پنل/API روی loopback:8081؛ Nginx عمومی:8080 مسیر `/connect` را به Xray loopback:10000 می‌فرستد؛ Hostim TLS را خاتمه می‌دهد.
- `ADMIN_PASSWORD` حداقل ۱۶ کاراکتر و `PUBLIC_DOMAIN` hostname لازم است؛ Volume جدید `/data`. `DEMO_MODE` در تولید ممنوع.
- تک‌مدیر، تک‌UUID؛ مرزبان نیست. لینک‌های جدید مسیر `/connect` دارند، نه `/vless` قدیمی.
- لاگ عملیاتی محدود در RAM؛ raw Xray output عمداً حذف می‌شود. سطح لاگ در UI لاگ خام قابل دانلود ایجاد نمی‌کند. کاربران آنلاین و حجم ترافیک اندازه‌گیری نشده‌اند.
- رمز، UUID و CSRF نباید در گزارش یا Git ثبت شوند. گزارش دامنه دارد. preview از رمز غیرمحرمانهٔ `demo-password-2026` استفاده می‌کند؛ هرگز برای تولید پیشنهاد نشود.
- سالم‌بودن HTTP/فرایند مساوی اتصال همراه اول نیست. ادعاهای قبلی «۴۰۴ یعنی تونل سالم» و «تفاوت DNS قطعاً poisoning است» نادرست یا بیش‌ازحد قطعی‌اند.
- ۷ تست HTTP واقعی در DEMO موفق بوده‌اند؛ `py_compile`، `bash -n` و diff check موفق‌اند.
- Docker این محیط موجود نیست؛ دانلود باینری Xray از release-assets با SSL_ERROR_SYSCALL شکست خورد. بیلد Docker، تست کانفیگ با باینری و تونل واقعی هنوز تأیید نشده‌اند. نسخه v26.3.27 و SHAها از GitHub API گرفته شدند.
- هیچ Deploy روی حساب Hostim توسط این ایجنت انجام نشده. نصب اپ جدا و بررسی راهنمای `docs/HOSTIM-DEPLOY.fa.md` قدم بعد است.

## روند ادامهٔ امن

1. اتصال GitHub، وضعیت شاخه و آخرین remote را بررسی کن.
2. درخواست کاربر و راهنمای نسخه‌ها را بخوان؛ برای تغییر بک‌اند ابتدا کد تأیید بگیر.
3. قبل از توصیهٔ جایگزینی سرویس سالم، یک app preview با Volume مستقل تست شود.
4. تست‌ها و محدودیت‌های تست را صریح گزارش کن؛ همهٔ تغییرات و docs را قبل از یک پوش نهایی آماده کن.
5. بعد از پوش، SHA remote را با HEAD تطبیق بده و در پاسخ تحویل ثبت کن؛ ادعای پوش را بدون تأیید remote نکن.

## بررسی تکمیلی توقف پنل — ۱۷ شهریور ۱۴۰۵ (بدون تغییر بک‌اند)

- اتصال GitHub تأیید و remote همان `216bf5be2e7c1fb325eea115162f919bac8f9fe3` بود؛ کاربر صحت Envs را تأکید کرده است. دوباره او را در حلقهٔ اصلاح دامنه قرار نده.
- `start.sh`، `nginx.conf` و ابتدای `app/server.py` بررسی شدند: اعتبارسنجی محیط قبل از ایجاد HTTP server انجام می‌شود. خروج پنل باعث خاتمهٔ اسکریپت supervisor و خاموش‌شدن Nginx می‌شود؛ بنابراین حتی صفحهٔ ورود هم در دسترس نیست.
- با اجرای خود ماژول از طریق runpy در محیط موقت: ورودی معتبر exit=0؛ رمز خالی یا دامنه با https هر دو exit=1 و همان پیام کلی را تولید کردند. هیچ مقدار واقعی کاربر در این آزمایش نبود.
- اجرای واقعی HTTP پنل با DEMO_MODE=0 و مقادیر آزمایشی معتبر، در localhost پاسخ 200 و فرم ورود داد. این تست Docker/Hostim یا Xray واقعی را تأیید نمی‌کند؛ نبود Xray توسط کد ثبت و مدیریت شد.
- curl به دامنهٔ کاربر در همین محیط SSL_ERROR_SYSCALL و status=000 داد؛ نتیجهٔ سلامت دامنه یا علت توقف از این آزمایش قابل استنتاج نیست.
- پیام فعلی ثابت نمی‌کند کدام مقدار به فرایند نرسیده/نامعتبر است. تفاوت محیط runtime با فیلد ذخیره‌شده یا اجرای قدیمی همچنان نامعلوم است.
- هیچ بک‌اندی تغییر نکرد و هیچ پوشی انجام نشد. پیشنهاد بعدی پیام تشخیصی جداگانه و بدون مقادیر محرمانه است؛ هنوز تأیید پنج‌رقمی کاربر برای آن دریافت نشده است.
